--- a/Source/Pcap_DNSProxy/CMakeLists.txt
+++ b/Source/Pcap_DNSProxy/CMakeLists.txt
@@ -64,6 +64,8 @@ set(MAIN_SRC_FILES
 		"ReadParameter.cpp"
 		"Service.cpp")
 
+# Check Pthread Library
+find_package(Threads REQUIRED)
 # Compiling and linking flags
 ## C++ 11 standard definitions
 IF (CMAKE_VERSION VERSION_LESS "3.1")
@@ -98,7 +100,6 @@ IF(PLATFORM_OPENWRT)
 ENDIF(PLATFORM_OPENWRT)
 
 # Libraries linking
-find_package(Threads REQUIRED)
 target_link_libraries(Pcap_DNSProxy ${CMAKE_THREAD_LIBS_INIT})
 IF(ENABLE_PCAP)
 	target_link_libraries(Pcap_DNSProxy pcap)
